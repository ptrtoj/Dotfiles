;;; libj-goto-addr.el -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:
(use-package goto-addr
  :diminish
  :config
  (goto-address-mode 1))

(provide 'libj-goto-addr)

;;; libj-goto-addr.el ends here
